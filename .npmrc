# Refuse to install with an npm outside package.json "engines" (the npm bundled
# with Node 22, which CI and the Docker image use). A different npm rewrites
# package-lock.json metadata and turns every install into lockfile noise.
engine-strict=true
